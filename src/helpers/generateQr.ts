// import QRCode from 'qrcode';

// const generateQR = async (text: string) => {
//   try {
//     const qr64 = await QRCode.toDataURL(text, { margin: 1 });

//     return qr64;
//   } catch (err) {
//     console.error(err);
//   }
// };

// export { generateQR };
