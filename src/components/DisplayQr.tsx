import { Card, CardContent, CardHeader, CardTitle } from '~/ui/card';

const DisplayQr = () => {
  // to display image
  return (
    <Card>
      <CardHeader>
        <CardTitle>Your Qr Code</CardTitle>
      </CardHeader>
      <CardContent>DisplayQr</CardContent>
    </Card>
  );
};

export default DisplayQr;
